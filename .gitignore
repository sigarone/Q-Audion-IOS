.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
*.xcodeproj
.build/

# Claude Code
.claude/

# Local cache (log fetches, build artifacts)
.cache/

# Code review tooling
.code-review-graph/

# Graphify (AST graph cache)
graphify-out/

# MASQUE: quiche QUIC/H3 backend — built in CI (or locally) by
# scripts/build-quiche-xcframework.sh, never committed (large binary).
# Reality (VLESS+REALITY over xray-core, scripts/build-reality-xcframework.sh)
# also lands here (Reality.xcframework) — same rule, no separate entry needed.
QAudionApp/Vendor/
.quiche-build/

# Local Stitch reference screenshots (not committed)
.stitch_*.png
.stitch_*.html

# Server admin scripts (belong in bcrypto-server, not iOS repo)
scripts/reset_profiles_vps.py
.worktrees/

# Python bytecode cache (generated by running scripts/*.py diagnostics)
__pycache__/
*.pyc

# WebRTC/onnxruntime are fetched as remote SPM binaryTargets (see
# QAudionEngine/Package.swift) — never as local zips. Two orphaned zips
# (WebRTC-M147.zip, pod-onnxruntime-ext-0.13.0.zip, unreferenced anywhere
# in the repo) were swept in by an unrelated commit (730457d) via a broad
# `git add`; removed 2026-07-28. This guards against a repeat.
/WebRTC-M147.zip
/pod-onnxruntime-ext-0.13.0.zip
